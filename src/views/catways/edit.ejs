<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Port Russell - Modifier un Catway</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <div class="dashboard">
        <div class="dashboard-header">
          <h1>⚓ Modifier le Catway #<%= catway.catwayNumber %></h1>
          <div class="dashboard-info">
            <span>👤 <%= user.username %></span>
          </div>
        </div>

        <nav class="dashboard-nav">
          <ul>
            <li><a href="/dashboard">🏠 Tableau de bord</a></li>
            <li><a href="/catways">⚓ Catways</a></li>
            <li><a href="/reservations">📋 Réservations</a></li>
            <li><a href="/users">👥 Utilisateurs</a></li>
            <li><a href="/api/logout">🚪 Déconnexion</a></li>
          </ul>
        </nav>

        <div class="dashboard-content">
          <% if (error) { %>
          <div class="error"><%= error %></div>
          <% } %>

          <form
            action="/catways/<%= catway.catwayNumber %>/edit"
            method="POST"
            style="max-width: 600px"
          >
            <div class="form-group">
              <label>Numéro du Catway</label>
              <input
                type="number"
                value="<%= catway.catwayNumber %>"
                disabled
                style="
                  background: #f0f0f0;
                  width: 100%;
                  padding: 12px;
                  border: 2px solid #e0e0e0;
                  border-radius: 8px;
                  font-size: 16px;
                "
              />
              <small style="color: #666"
                >Le numéro ne peut pas être modifié</small
              >
            </div>

            <div class="form-group">
              <label>Type</label>
              <input
                type="text"
                value="<%= catway.catwayType %>"
                disabled
                style="
                  background: #f0f0f0;
                  width: 100%;
                  padding: 12px;
                  border: 2px solid #e0e0e0;
                  border-radius: 8px;
                  font-size: 16px;
                "
              />
              <small style="color: #666"
                >Le type ne peut pas être modifié</small
              >
            </div>

            <div class="form-group">
              <label for="catwayState">État *</label>
              <textarea
                id="catwayState"
                name="catwayState"
                required
                rows="4"
                style="
                  width: 100%;
                  padding: 12px;
                  border: 2px solid #e0e0e0;
                  border-radius: 8px;
                  font-size: 16px;
                "
              >
<%= catway.catwayState %></textarea
              >
              <small style="color: #666">Seul l'état peut être modifié</small>
            </div>

            <div style="display: flex; gap: 10px">
              <button type="submit" class="btn">Enregistrer</button>
              <a
                href="/catways"
                class="btn"
                style="
                  background: #6c757d;
                  text-align: center;
                  text-decoration: none;
                  display: inline-block;
                "
                >Annuler</a
              >
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
