<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Port Russell - Réservations du Catway #<%= catway.catwayNumber %>
    </title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <div class="dashboard">
        <div class="dashboard-header">
          <h1>📋 Réservations du Catway #<%= catway.catwayNumber %></h1>
          <div class="dashboard-info">
            <span>👤 <%= user.username %></span>
          </div>
        </div>

        <nav class="dashboard-nav">
          <ul>
            <li><a href="/dashboard">🏠 Tableau de bord</a></li>
            <li><a href="/catways">⚓ Catways</a></li>
            <li><a href="/reservations">📋 Réservations</a></li>
            <li><a href="/users">👥 Utilisateurs</a></li>
            <li><a href="/api/logout">🚪 Déconnexion</a></li>
          </ul>
        </nav>

        <div class="dashboard-content">
          <div
            style="
              background: #f8f9fa;
              padding: 20px;
              border-radius: 10px;
              margin-bottom: 20px;
            "
          >
            <h3>Informations du Catway</h3>
            <p><strong>Numéro :</strong> #<%= catway.catwayNumber %></p>
            <p><strong>Type :</strong> <%= catway.catwayType %></p>
            <p><strong>État :</strong> <%= catway.catwayState %></p>
          </div>

          <div
            style="
              display: flex;
              justify-content: space-between;
              align-items: center;
              margin-bottom: 20px;
            "
          >
            <h2>Réservations (<%= reservations.length %>)</h2>
            <a
              href="/catways"
              class="btn btn-sm"
              style="width: auto; background: #6c757d"
              >← Retour aux catways</a
            >
          </div>

          <% if (reservations.length === 0) { %>
          <p style="text-align: center; color: #999; padding: 30px">
            Aucune réservation pour ce catway
          </p>
          <% } else { %>
          <table>
            <thead>
              <tr>
                <th>Client</th>
                <th>Bateau</th>
                <th>Date début</th>
                <th>Date fin</th>
                <th>Statut</th>
              </tr>
            </thead>
            <tbody>
              <% reservations.forEach(function(reservation) { var now = new
              Date(); var start = new Date(reservation.startDate); var end = new
              Date(reservation.endDate); var status = ''; if (now < start) {
              status = '🔵 À venir'; } else if (now >= start && now <= end) {
              status = '🟢 En cours'; } else { status = '⚫ Terminée'; } %>
              <tr>
                <td><%= reservation.clientName %></td>
                <td><%= reservation.boatName %></td>
                <td>
                  <%= new
                  Date(reservation.startDate).toLocaleDateString('fr-FR') %>
                </td>
                <td>
                  <%= new Date(reservation.endDate).toLocaleDateString('fr-FR')
                  %>
                </td>
                <td><%= status %></td>
              </tr>
              <% }); %>
            </tbody>
          </table>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
